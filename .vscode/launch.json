{
  "configurations": [
    {
      "name": "Python (dataGenerator)",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "python": "${workspaceFolder}/dataGenerator/.venv/bin/python",
      "console": "integratedTerminal"
    }
  ]
}