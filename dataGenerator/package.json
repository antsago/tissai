{
  "name": "tangledrip",
  "packageManager": "yarn@3.6.3",
  "scripts": {
    "db": "node src/generateDb",
    "relationships": "python src/generateRelationships.py > ../data/relationships.json",
    "test": "jest",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "jest": "^29.6.4",
    "prettier": "3.0.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "robots-parser": "^3.0.1",
    "xml2js": "^0.6.2"
  }
}
